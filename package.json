{
  "name": "flowsupply",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter server run dev\" \"pnpm --filter client run dev\"",
    "build": "pnpm --filter client run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
