name = "flowsupply"
compatibility_date = "2026-05-04"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# Set VITE_API_URL in the Worker dashboard (Settings → Variables and Secrets)
# to the deployed API Worker URL, e.g. https://flowsupply-api.your-subdomain.workers.dev
